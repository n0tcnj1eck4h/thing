#include "fps_cam_ctl.h"
#include "GLFW/glfw3.h"

void fps_camera_controller_update(FPSCameraController* controller, double delta) {
    //glm_vec3_rotate(controller -> camera -> pos, delta * GLM_PI, GLM_YUP);
}
